# rust-templates

This repo contains various templates for rust projects. Templates are organized in branches.

## Usage

To use one of the branches of this repo as template with `cargo-generate`:

```bash
cargo generate lmeller-git/rust-templates --branch <branch> --name <target-name>
```
