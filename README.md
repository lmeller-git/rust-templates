# rust-template-lib

This repository contains an opinionated template for rust libraries. It is aimed at concurrency related libraries. Some workflows may have to be adjusted for your usecase.

## Usage

This repo may be used as a template via github templates or cargo-generate.

To use the latter:

```bash
cargo generate lmeller-git/rust-template-lib --name <your-crate-name>
```
